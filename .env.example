# ================== Postgres 配置（容器内） ==================
POSTGRES_DB=windsurf_pool
POSTGRES_USER=your_db_user
POSTGRES_PASSWORD=your_db_password

# ================== 应用数据库连接串 ==================
# 注意 host 用的是 db（docker-compose 里的服务名），不是 localhost
DATABASE_URL=postgresql://your_db_user:your_db_password@db:5432/windsurf_pool

# ================== 管理面板账号 ==================
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_this_password

# ================== 后端签名密钥 ==================
SECRET_KEY=change-this-to-a-random-secret-key

# ================== 内部上传密钥 ==================
# 要和 windsurf-create.py 里的 INTERNAL_TOKEN 一样
INTERNAL_UPLOAD_TOKEN=change_this_internal_token

# ================== 账号过期配置 ==================
# 未使用账号自动过期天数（默认6天）
ACCOUNT_EXPIRY_DAYS=6

# ================== Firebase API Key（可选） ==================
# 用于账号密码登录功能，如果不设置会自动从网页获取
# FIREBASE_API_KEY=your_firebase_api_key_here
